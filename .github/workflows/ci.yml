name: CI Pipeline

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Set up Java with Adoptium (correct distribution name)
      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adoptium'  # Corrected distribution name

      # Run unit tests using Maven (adjust for your build tool)
      - name: Run tests
        run: |
          mvn install
          mvn test
